name: Workflow Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write

    env:
      SERVER_PORT: 8080
      DB_HOST: 127.0.0.1
      DB_PORT: 3306
      DB_NAME: grotrack
      DB_USERNAME: root
      DB_PASSWORD: 123456
      JWT_VALIDITY: 3600000
      JWT_SECRET: "RXhpc3RlIHVtYSB0ZW9yaWEgcXVlIGRpeiBxdWUsIHNlIHVtIGRpYSBhbGd16W0gZGVzY29icmlyIGV4YXRhbWVudGUgcGFyYSBxdW..."

    services:
      mysql:
        image: henriquedandrade/bd-grotrack-v2:bd
        env:
          MYSQL_ROOT_PASSWORD: 123456
          MYSQL_DATABASE: grotrack
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Importante para o SonarQube analisar o histórico de commits

      - name: Configurar JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Cache de Banco de Dados NVD (Dependency Check)
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository/org/owasp/dependency-check-data
          key: dependency-check-data-${{ runner.os }}
          restore-keys: |
            dependency-check-data-${{ runner.os }}

      - name: Dependency Check
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        continue-on-error: true
        run: |
          echo "🔍 Iniciando Dependency Check..."
          
          # Definir dados directory para cache
          export DEPENDENCY_CHECK_HOME="$HOME/.dependency-check"
          mkdir -p "$DEPENDENCY_CHECK_HOME/data"
          
          # Configuração comum
          COMMON_OPTS="-DskipProvidedScope=true -DskipRuntimeScope=false -DskipTestScope=true -DcacheDirectory=$DEPENDENCY_CHECK_HOME/data"
          
          # Tentar com API Key se disponível
          if [ ! -z "$NVD_API_KEY" ]; then
            echo "✓ NVD_API_KEY detectada. Tentando com API..."
            mvn org.owasp:dependency-check-maven:check \
              -DnvdApiKey=$NVD_API_KEY \
              $COMMON_OPTS \
              -DcveUrl12Modified=https://nvd.nist.gov/feeds/json/cve/1.1/nvdcve-1.1-modified.json \
              -DcveUrl20Modified=https://nvd.nist.gov/feeds/json/cve/2.0/nvdcve-2.0-modified.json \
              -Dretriever=ASYNC && exit 0
            echo "⚠️  Tentativa com API falhou. Tentando fallback..."
          fi
          
          # Fallback: modo offline com cache
          echo "📦 Rodando Dependency Check em modo offline..."
          mvn org.owasp:dependency-check-maven:check \
            $COMMON_OPTS \
            -Dformat=JSON \
            -Dretriever=ASYNC || {
            echo "⚠️  Dependency Check não conseguiu atualizar banco de dados."
            echo "    Continuando build (será corrigido quando API Key for configurada)..."
            exit 0
          }

      - name: Executar Build e Testes com Maven (JaCoCo)
        # O verify garante que o JaCoCo gere o relatório xml antes do Sonar rodar
        run: mvn clean verify -DskipTests=false

      - name: Validar Configuração SonarQube
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          echo "🔍 Validando configuração SonarQube..."
          if [ -z "$SONAR_TOKEN" ]; then
            echo "⚠️  SONAR_TOKEN não configurado!"
            echo "    Por favor, adicione ao GitHub Secrets"
            exit 1
          fi
          echo "✓ SONAR_TOKEN configurado"
          echo "✓ Organização: grotrack"
          echo "✓ Projeto: grotrack_back-end"

      - name: Executar SonarQube Scan
        if: always() # Garante que o scan rode mesmo se um teste falhar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true
        run: >
          mvn sonar:sonar 
          -Dsonar.projectKey=grotrack_back-end 
          -Dsonar.organization=grotrack 
          -Dsonar.host.url=https://sonarcloud.io 
          -Dsonar.login=$SONAR_TOKEN 
          -Dsonar.sources=src/main/java 
          -Dsonar.tests=src/test/java

      - name: Upload do Relatório de Cobertura JaCoCo
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco/
          retention-days: 7